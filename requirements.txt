Django>=5.0,<6.0
numpy>=1.24
openai-whisper>=20231117
# Cloud generation providers:
openai>=1.66
# Optional cloud providers:
# anthropic>=0.30
jiwer>=3.0
SQLAlchemy>=1.4
cryptography>=42.0
requests>=2.31
soundfile>=0.12
pydub>=0.25.1
# if running python 3.12 or lower, and have an Nvidia GPU you can install CUDA enabled torch to run whisper on:
# (GPU): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu12

# Ensure ffmpeg is on PATH for Whisper

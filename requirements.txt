PyQt5
sounddevice
numpy
pyqtgraph
openai-whisper
jiwer
sqlalchemy # orm to avoid direct SQL queries
pyinstaller # for packaging into an executable
# package with pyinstaller -y --windowed --name SpeechPractice --add-data "scripts;scripts" --add-data "script_index.json;." SpeechPractice.py
# ensure ffmpeg is on your PATH for whisper
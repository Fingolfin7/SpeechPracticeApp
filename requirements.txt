Django>=5.0,<6.0
dj-database-url>=2.2
django-storages[s3]>=1.14
gunicorn>=23.0
psycopg[binary]>=3.2
whitenoise>=6.8
numpy>=1.24
# Cloud generation providers:
openai>=1.66
# Optional cloud providers:
# anthropic>=0.30
jiwer>=3.0
cryptography>=42.0
requests>=2.31
# Local Whisper is optional and lives in requirements-local.txt. Production
# sends normal browser-recorded WebM files directly to OpenAI.
